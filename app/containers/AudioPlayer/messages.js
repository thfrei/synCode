/*
 * AudiPlayer Messages
 *
 * This contains all the text for the AudiPlayer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AudiPlayer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AudiPlayer container!',
  },
});
