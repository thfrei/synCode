/*
 * SidebarSettings Messages
 *
 * This contains all the text for the SidebarSettings container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SidebarSettings';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SidebarSettings container!',
  },
});
