/*
 * Grid Messages
 *
 * This contains all the text for the Grid container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Grid';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Grid container!',
  },
});
