/*
 * Control Messages
 *
 * This contains all the text for the Control container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Control';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Control container!',
  },
});
